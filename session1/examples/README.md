# Session 1 Examples

`clock_ticks.c`: Program to display the number of clock ticks per second
(note: system-dependent)

`multithreaded.c`: Program that demonstrates how CPU time can exceed wall time
when there are multiple threads of execution.
